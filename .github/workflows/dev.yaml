name: github actions dev test
on:
  pull_request:
    branches:
      - main
    types: 
      - opened
      - synchronize

jobs:
  dev:
    runs-on: ubuntu-latest
    steps:
    - run: |
        echo The PR was merged
